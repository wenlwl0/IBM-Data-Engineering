SELECT ROUND(COST) FROM PETRESCUE;
SELECT ROUND(COST, 0) FROM PETRESCUE;
SELECT ROUND(COST, 2) FROM PETRESCUE;
--query that displays the rounded integral cost of each rescue

SELECT LENGTH(ANIMAL) FROM PETRESCUE;
--query that displays the length of each animal name

SELECT UCASE(ANIMAL) FROM PETRESCUE;
--query that displays the animal name in each rescue in uppercase

SELECT LCASE(ANIMAL) FROM PETRESCUE;
--lowercase

