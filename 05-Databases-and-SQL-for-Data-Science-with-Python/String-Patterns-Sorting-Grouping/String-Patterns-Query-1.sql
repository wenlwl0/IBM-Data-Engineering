SELECT F_NAME, L_NAME
FROM EMPLOYEES
WHERE ADDRESS LIKE '%Elgin,IL%';

--Retrieve the first names F_NAME and last names L_NAME of all employees who live in Elgin,IL. LIKE operator to retrieve strings that contain the said text. 